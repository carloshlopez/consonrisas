<h1><%= I18n.t('events.list') %></h1>

<table>
    <th></th>
    <th><%= I18n.t('date_name')%></th>
    <th><%= I18n.t('name') %></th>
    <% if member_signed_in? %>
      <th> <%= I18n.t('actions') %> </th>
    <% end %>
  </tr>
<% num = 0 %>
<% @events.each do |event| %>
  <% if num % 2 == 0 %>
  <tr>
  <% else %>
  <tr class="alt">
  <% end %>
    <td> <%= image_tag event.pic.url(:thumb) %> </td>
    <td><%= event.date %></td>
    <td><%= event.name %></td>
    <% if member_signed_in? %>
      <td>
        <%= link_to I18n.t('events.show'), event %>
        | <%= link_to I18n.t('edit'), edit_event_path(event) %>
        | <%= link_to I18n.t('destroy'), event, :confirm => I18n.t('messages.delete_confirmation'), :method => :delete %>
      </td>
    <% end %>
  </tr>
<% 
  num = num + 1
  end 
%>
</table>

<br />

<%= link_to I18n.t('events.new'), new_event_path %>
