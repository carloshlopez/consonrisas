#left-div-lists
  = render :partial => "shared/create_buttons"
  .vip.vip-fundations
    - new = News.where("news_type = ? AND actual = ?", 1, true).order("updated_at DESC").first();
    - if new
      = image_tag  '/images/FundacionesTransparenteChiqui.png', :class=>"transparent"
      %h2
        Destacados
      .type
        proyecto social
      = image_tag "#{new.pic.url(:profile)}", :class=>"news-img"
      .text
        .news-title
          = new.title
        .news-fecha
          = I18n.l(new.date, :format=>:long)
        %p.news
          = new.news.size > 400 ? new.news[0 .. 400] + "..." : new.news
          .news-link
            %a.news-more{:href=>"#"}ver más    
#list-content
  - @fund_list_title = I18n.t('fundation.list')
  = render :partial => "list"
  .pagination-fundations 
    = paginate @fundations      
