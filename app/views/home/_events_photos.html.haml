#makeMeScrollable
  .scrollingHotSpotLeft
  .scrollingHotSpotLeftVisible
  .scrollingHotSpotRight
  .scrollingHotSpotRightVisible
  .scrollWrapper
    .scrollableArea
      - @photos.each do |p|
        = image_tag(p.photo.url(:original), :class=>"home_pic")
