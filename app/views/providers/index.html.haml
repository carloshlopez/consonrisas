#left-div-lists
  = render :partial => "shared/create_provider_button"
  .vip.vip-providers
    - new = News.where("news_type = ? AND actual = ?", 2, true).order("updated_at DESC").first();
    - if new
      = image_tag  '/images/ProveedorTransparenteChiqui.png', :class=>"transparent"
      %h2
        Destacados
      .type
        proveedor
      = image_tag "#{new.pic.url(:profile)}", :class=>"news-img"
      .text
        .news-title
          = new.title
        .news-fecha
          = I18n.l(new.date, :format=>:long)
        %p.news
          = new.news.size > 400 ? new.news[0 .. 400] + "..." : new.news
          .news-link
            %a.news-more{:href=>"#"}ver más
#list-content
  - @prov_list_title = I18n.t('provider.list')
  = render :partial => "list"
  .pagination-providers
    = paginate @providers  
