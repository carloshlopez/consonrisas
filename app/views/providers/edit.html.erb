<h1 class="title"><%= I18n.t('provider.edit_title') %></h1>

<% if can_current_member_edit_provider(@provider.id) %>
<%= render 'form' %>
<% end %>

<%= link_to 'Show', @provider %> |
<%= link_to 'Back', providers_path %>
