<h1>Listing need_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @need_categories.each do |need_category| %>
  <tr>
    <td><%= need_category.name %></td>
    <td><%= link_to 'Show', need_category %></td>
    <td><%= link_to 'Edit', edit_need_category_path(need_category) %></td>
    <td><%= link_to 'Destroy', need_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Need category', new_need_category_path %>
