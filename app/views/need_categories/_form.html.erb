<%= form_for(@need_category) do |f| %>
  <% if @need_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@need_category.errors.count, "error") %> prohibited this need_category from being saved:</h2>

      <ul>
      <% @need_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
