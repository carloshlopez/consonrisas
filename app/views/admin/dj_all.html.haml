- if current_member.try(:admin?)
  %h2 DelayedJobs
  .manage_workers
    %a{:href=>"workers"}
      Manage Heroku Workers  
  .all_jobs
    - @jobs.each do |j|
      %p
        %a{:href=>"/admin/dj_show/#{j.id}"}
          ="ID: #{j.id}"
- else
  %p YOU HAVE TO BE AN ADMIN TO VIEW THIS
